module enums;

enum TokenType : char {
    // Groupers
    LEFT_PARAN = '(',
    RIGHT_PARAN = ')',

    // Values
    NUMBER = '1',

    // Operators
    PLUS = '+',
    MINUS = '-',
    DIVIDE = '/',
    MULTIPLY = '*',
    POWER = '^',
}
